@startuml
title Player-Role

skinparam legendBackgroundColor #f9f9f9
skinparam legendBorderColor #999999

legend right
**Intent**: Allow objects (Players) to acquire/drop dynamic roles at runtime without changing the player’s class.

**Use when**: Context-dependent behavior (e.g., user roles, game character roles).

**Key participants**: Player, Role, ConcreteRole, RoleBinding.
endlegend

class Player {
  -roles : Map<String, Role>
  +attach(role: Role)
  +detach(roleName: String)
  +perform(action: String)
}

interface Role {
  +supports(action: String) : boolean
  +execute(player: Player, action: String)
}

class AdminRole
class GuestRole
class EditorRole

Role <|.. AdminRole
Role <|.. GuestRole
Role <|.. EditorRole

Player o-- Role : dynamic composition
@enduml